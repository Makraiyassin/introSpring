spring:

  datasource:
    url: jdbc:h2:mem:demoBF2022 #"jdbc:mysql://localhost:3306"
    username: sa
    password: "1234"
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect

server:
  port: 8081